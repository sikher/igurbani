{
  "name": "gurbanidb-sample-app",
  "version": "0.0.2",
  "devDependencies": {
    "vite": "^5.0.5"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 3000",
    "deploy:gh-pages": "./scripts/deploy.sh"
  }
}
